BoxPane "TreePane" {
  CONSTANTS: [
    IMAGE:              "CLONE \"T_ImagePane\" \"Image\"",
    VIEW_BUTTON_VALS:   "min_size: 6 6, $RESIZE_WH",
    ACTION_BUTTON_SIZE: "min_size: 10 10",
  ],

  TEMPLATES: [
    CLONE "T_ButtonPane" "T_ViewButton" {
      border: $BORDER { width: 1, color: "#666666" },
    },

    # Switcher for visibility buttons.
    SwitcherPane "T_VisSwitcher" {
      resize_height: True,
      panes: [
        $IMAGE { $VIEW_BUTTON_VALS, path: "DotIcon.jpg" },  # No-op
        CLONE "T_ViewButton" "HideButton" {
          panes: [ $IMAGE { $VIEW_BUTTON_VALS, path: "VisibleIcon.jpg" } ]
        },
        CLONE "T_ViewButton" "ShowButton" {
          panes: [ $IMAGE { $VIEW_BUTTON_VALS, path: "InvisibleIcon.jpg" } ]
        },
      ]
    },

    # Switcher for expand/collapse buttons.
    SwitcherPane "T_ExpSwitcher" {
      resize_height: True,
      panes: [
        $IMAGE { $VIEW_BUTTON_VALS, path: "DotIcon.jpg" },  # No-op
        CLONE "T_ViewButton" "CollapseButton" {
          panes: [ $IMAGE { $VIEW_BUTTON_VALS, path: "CollapseIcon.jpg" } ]
        },
        CLONE "T_ViewButton" "ExpandButton" {
          panes: [ $IMAGE { $VIEW_BUTTON_VALS, path: "ExpandIcon.jpg" } ]
        },
      ]
    },

    # Button used to show the Model name and select it.
    CLONE "T_ButtonPane" "T_ModelButton" {
      border: $BORDER { width: 0 },
      resize_width:  True,
      resize_height: True,
      panes: [
        CLONE "T_TextPane" "Text" {
          resize_width: True,
          resize_height: True,
          halignment: "kAlignLeft",
        }
      ]
    },
  ],

  background: $BG { color: "#e4e4e4" },
  min_size: 150 200,
  padding: 2,
  spacing: 2,
  panes: [
    # The session row is outside the scrolling area so it is always visible.
    BoxPane "SessionRow" {
      background: $BG     { color: "#e4bbe4" },
      border:     $BORDER { width: 1 },
      orientation: "kHorizontal",
      resize_width:  True,
      padding: 1,
      min_size: 1 12,
      panes: [
        CLONE "T_VisSwitcher" "VisSwitcher" {},
        CLONE "T_TextPane" "SessionString" {
          resize_width: True,
          resize_height: True,
          halignment: "kAlignLeft",
        },
      ]
    },

    # Basis for creating each Model row in the scrolling area.
    BoxPane "ModelRow" {
      disabled_flags: "kTraversal",
      orientation: "kHorizontal",
      resize_width:  True,
      min_size: 1 10,
      panes: [
        CLONE "T_VisSwitcher" "VisSwitcher" {},
        CLONE "T_ExpSwitcher" "ExpSwitcher" {},
        SpacerPane "Spacer" {},  # Indents the next button and Model name.
        CLONE "T_ModelButton" "ModelButton" {},
      ]
    }

    # Scrolling tree view of Models.
    ScrollingPane "Scroller" {
      resize_width:  True,
      resize_height: True,
      contents: CLONE "T_ClipPane" "Contents" {
        border: $BORDER { width: 1 },
        padding: 2,
        spacing: 0,
        resize_width:  True,
        resize_height: True,
      },
    },

    # Action buttons.
    BoxPane {
      orientation: "kHorizontal",
      resize_width:  True,
      panes: [
        SpacerPane { resize_width: True },
        CLONE "T_ButtonPane" "EditName" {
          border: $BORDER { width: 2 },
          panes: [ $IMAGE { $ACTION_BUTTON_SIZE, path: "EditNameIcon.jpg" } ]
        },
        SpacerPane { resize_width: True },
        CLONE "T_ButtonPane" "MoveUp" {
          border: $BORDER { width: 2 },
          panes: [ $IMAGE { $ACTION_BUTTON_SIZE, path: "UpIcon.jpg" } ]
        },
        SpacerPane { resize_width: True },
        CLONE "T_ButtonPane" "MoveDown" {
          border: $BORDER { width: 2 },
          panes: [ $IMAGE { $ACTION_BUTTON_SIZE, path: "DownIcon.jpg" } ]
        },
        SpacerPane { resize_width: True },
      ]
    },
  ]
},
