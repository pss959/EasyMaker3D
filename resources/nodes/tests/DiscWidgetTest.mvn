Node "DiscWidgetTest" {
  TEMPLATES: [
    Torus "T_DiscShape" {
      outer_radius: 10,
      inner_radius: 1,
      sector_count: 7,
    }
  ],

  # Rotate the whole setup.
  #rotation: 1 1 -1 24,

  children: [
    Node {  # Background square.
      translation: 0 0 -14,
      shapes: [ Box { size: 40 40 .1 } ],
    },

    Node "XRotator" {
      rotation: 0 0 1 -90,
      children: [
        DiscWidget "XRotator" {
          scaling_allowed: False,
          inactive_color: "#ff3333",
          shapes: [ CLONE "T_DiscShape" {} ],
        },
      ]
    },

    Node "YRotator" {
      children: [
        DiscWidget "XRotator" {
          scaling_allowed: False,
          inactive_color: "#33ff33",
          shapes: [ CLONE "T_DiscShape" {} ],
        },
      ]
    },

    Node "ZRotator" {
      rotation: 1 0 0 90,
      children: [
        DiscWidget "XRotator" {
          scaling_allowed: False,
          inactive_color: "#3333ff",
          shapes: [ CLONE "T_DiscShape" {} ],
        },
      ]
    },
  ]
}
