//=============================================================================
// Icon used for the Clip Tool.
// =============================================================================

module ClipIcon() {
     cylinder(r = 5, h = 12, center = true);
     rotate([20, 0, 0]) cube([18, 18, 1], center = true);
}

ClipIcon($fn = 40);
