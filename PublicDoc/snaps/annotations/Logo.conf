# Creates the small Logo image from the regular one. Note that the result size
# here should not change too much because the CSS for the doc uses fixed
# positioning below the logo image.

read   static/Logo.jpg
resize 50 50
write  static/LogoSmall.jpg

# Also create the favicon.
resize 32 32
write  static/favicon.ico
