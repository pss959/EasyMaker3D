#pragma once

/// The Axis enum represents one of the three principal axes. Note that
/// depending on context this may refer to application coordinates (Y up) or
/// user coordinates (Z up).
///
/// \ingroup Enums
enum class Axis {
    kX,
    kY,
    kZ,
};
