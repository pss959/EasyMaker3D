- Move all SG Math stuff into Math.h. Leave Hit out.

- Add Bounds to Node and Shape (cached, with flag).
- Add flags for what changed.
- Add notification to Node from shapes and children
     - (all types from Model.Change)
     - Invalidate caches, matrices, etc...
- Update traversal fixes all broken caches.

- Intersector traversal tests bounds first (in local coords)
